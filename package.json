{
  "name": "reku-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Static site; no build step'",
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs",
    "test:api": "node --test --test-concurrency=1 integration/agreement-api-flow.test.mjs",
    "test:integration": "node --test --test-concurrency=1 integration/*.test.mjs",
    "check:ci": "npm run check && npm run test:integration",
    "check": "node --check server.mjs && find src scripts test integration -name '*.mjs' -print0 | xargs -0 -n1 node --check && node --check admin/app.js && node --check agenda/app.js && node --check profesional-turnos/app.js && node --check profesional/app.js && node --check congreso-cokiba/app.js && npm test && npm run secrets:check",
    "secrets:check": "bash scripts/secrets_check.sh",
    "google:backfill-attendees": "node scripts/backfill-google-calendar-attendees.mjs",
    "web-push:keys": "node scripts/generate-web-push-keys.mjs"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "pdfkit": "^0.17.2",
    "pg": "^8.22.0",
    "qrcode": "^1.5.4",
    "sharp": "^0.35.4",
    "web-push": "^3.6.7"
  }
}
